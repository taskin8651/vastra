@extends('layouts.admin')
@section('page-title', $audience->exists ? 'Edit Audience' : 'Add Audience')
@section('content')
<div class="admin-page-head"><div><a class="admin-back-link" href="{{ route('admin.audiences.index') }}">← Back to list</a><h2 class="admin-page-title">{{ $audience->exists ? 'Edit' : 'Add' }} Audience</h2></div></div>
<form method="POST" action="{{ $audience->exists ? route('admin.audiences.update', $audience) : route('admin.audiences.store') }}">@csrf @if($audience->exists) @method('PUT') @endif <div class="form-card"><div class="form-card-body"><div class="field-group"><label class="field-label">Name *</label><input class="field-input" name="name" value="{{ old('name', $audience->name) }}" required></div><div class="field-group"><label class="field-label">Slug</label><input class="field-input" name="slug" value="{{ old('slug', $audience->slug) }}" placeholder="auto-generated if empty"></div><div class="field-group"><label class="field-label">Display order</label><input class="field-input" type="number" min="0" name="sort_order" value="{{ old('sort_order', $audience->sort_order ?? 0) }}"></div><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $audience->exists ? $audience->is_active : true))> Active</label></div></div><div class="form-actions"><button class="btn-primary">Save Audience</button><a class="btn-ghost" href="{{ route('admin.audiences.index') }}">Cancel</a></div></form>
@endsection
