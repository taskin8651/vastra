@extends('layouts.admin')
@section('page-title', $product->exists ? 'Edit Product' : 'Add Product')
@section('content')
<div class="admin-page-head"><div><a class="admin-back-link" href="{{ route('admin.products.index') }}">← Back to list</a><h2 class="admin-page-title">{{ $product->exists ? 'Edit' : 'Add' }} Product</h2></div></div><form method="POST" action="{{ $product->exists ? route('admin.products.update', $product) : route('admin.products.store') }}">@csrf @if($product->exists) @method('PUT') @endif <div class="form-card"><div class="form-card-body"><div class="field-group"><label class="field-label">Category *</label><select class="field-input" name="category_id" required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $product->category_id) == $category->id)>{{ $category->audience->name }} — {{ $category->name }}</option>@endforeach</select></div><div class="field-group"><label class="field-label">Brand *</label><select class="field-input" name="brand_id" required>@foreach($brands as $brand)<option value="{{ $brand->id }}" @selected(old('brand_id', $product->brand_id) == $brand->id)>{{ $brand->name }}</option>@endforeach</select></div><div class="field-group"><label class="field-label">Name *</label><input class="field-input" name="name" value="{{ old('name', $product->name) }}" required></div><div class="field-group"><label class="field-label">SKU *</label><input class="field-input" name="sku" value="{{ old('sku', $product->sku) }}" required></div><div class="field-group"><label class="field-label">Slug</label><input class="field-input" name="slug" value="{{ old('slug', $product->slug) }}"></div><div class="field-group"><label class="field-label">Price *</label><input class="field-input" type="number" step=".01" min="0" name="price" value="{{ old('price', $product->price) }}" required></div><div class="field-group"><label class="field-label">Old price</label><input class="field-input" type="number" step=".01" min="0" name="compare_at_price" value="{{ old('compare_at_price', $product->compare_at_price) }}"></div><div class="field-group"><label class="field-label">Stock *</label><input class="field-input" type="number" min="0" name="stock_quantity" value="{{ old('stock_quantity', $product->stock_quantity ?? 0) }}" required></div><div class="field-group"><label class="field-label">Image path</label><input class="field-input" name="image_path" value="{{ old('image_path', $product->image_path) }}" placeholder="assets/images/product-man.png"></div><div class="field-group"><label class="field-label">Description</label><textarea class="field-input" name="description" rows="5">{{ old('description', $product->description) }}</textarea></div><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $product->exists ? $product->is_active : true))> Active</label> &nbsp; <label><input type="checkbox" name="is_featured" value="1" @checked(old('is_featured', $product->is_featured))> Featured on home</label></div></div><div class="form-actions"><button class="btn-primary">Save Product</button><a class="btn-ghost" href="{{ route('admin.products.index') }}">Cancel</a></div></form>
@endsection
